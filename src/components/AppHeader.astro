---
const { title} = Astro.props;

// Define TypeScript interface for props
interface Props {
  title: string;
}
---
<!-- 
<header class="shadow-lg mb-5 sticky top-10 z-10 bg-white">
    <div class="m-auto p-6 max-w-xl flex items-center gap-4">
      <div>
        <slot name="icon"/>
      </div>
      <h1 class="font-bold text-lg">{title}</h1>
    </div>
  </header> -->
  <header>
    <div class="fixed top-10 flex items-center gap-2 px-4 py-6 mx-auto max-w-7xl w-full sm:px-6 lg:px-8 bg-white dark:bg-dark-eval-1 overflow-visible">
      <div>
        <slot name="icon"/>
      </div>
      <h1 class="font-bold text-lg">{title}</h1>
    </div>
</header>