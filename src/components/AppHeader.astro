---
const { title} = Astro.props;

// Define TypeScript interface for props
interface Props {
  title: string;
}
---
<!-- 
<header class="shadow-lg mb-5 sticky top-10 z-10 bg-white">
    <div class="m-auto p-6 max-w-xl flex items-center gap-4">
      <div>
        <slot name="icon"/>
      </div>
      <h1 class="font-bold text-lg">{title}</h1>
    </div>
  </header> -->
  <div class="flex flex-col gap-4 md:flex-row md:items-center md:justify-between">
    <div class="p-6 max-w-xl flex items-center gap-4">
      <div>
        <slot name="icon"/>
      </div>
      <h1 class="font-bold text-lg">{title}</h1>
    </div>
</div>